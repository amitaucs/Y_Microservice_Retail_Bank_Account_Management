spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: retail-bank-config-server
  profiles:
    active: prod
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
  endpoint:
    env:
      post:
        enabled: true

